{
  "name": "component-reviewer-mcp",
  "version": "2.0.0",
  "description": "MCP server for reviewing React component quality, TypeScript errors, tests, and accessibility",
  "type": "module",
  "main": "build/index.js",
  "bin": {
    "component-reviewer": "build/index.js"
  },
  "scripts": {
    "build": "tsc && node -e \"require('fs').chmodSync('build/index.js', '755')\"",
    "dev": "tsc --watch",
    "start": "node build/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@tools/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
