// Router module — Hash-based slide routing
export { parseHash, buildHash, onHashChange, setHash, getCurrentHash } from "./hash-router";
export type { HashState } from "./hash-router";
export { useRouter } from "./use-router";
export type { RouterState, RouterConfig } from "./use-router";
