/**
 * Sheet component variant definitions using class-variance-authority
 * @module Sheet
 */
/**
 * CVA variant definitions for Sheet component.
 * Provides styling for different slide directions (side).
 *
 * @example
 * ```tsx
 * import { sheetVariants } from "./Sheet.variants"
 *
 * // Use variants directly
 * const className = sheetVariants({ side: "left" })
 * ```
 */
export declare const sheetVariants: (props?: ({
    side?: "left" | "right" | "top" | "bottom" | null | undefined;
} & import("class-variance-authority/types").ClassProp) | undefined) => string;
//# sourceMappingURL=Sheet.variants.d.ts.map